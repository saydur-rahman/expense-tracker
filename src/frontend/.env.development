# Aspire injects these at runtime when started via the AppHost.
# They are here as a fallback for running `npm run dev` standalone.
VITE_API_BASE_URL=http://localhost:5080
VITE_AUTH_BASE_URL=http://localhost:5090
